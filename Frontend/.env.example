# ----------------------------------------------------------------------------
# Frontend (public site) environment variables
# Copy this file to ".env" and fill in the real value. Never commit ".env".
# ----------------------------------------------------------------------------

# Neon Postgres connection string — MUST be the SAME database the dashboard uses,
# so the public site shows content edited in the dashboard.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require"
